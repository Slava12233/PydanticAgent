from setuptools import setup, find_packages

setup(
    name="pydantic_agent",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # כאן יש לציין את כל התלויות של הפרויקט
        # לדוגמה: "pydantic>=2.0.0"
    ],
)
